node_modules/
.expo/
dist/
# The prebuild output only — an unanchored `ios/` also matched
# frontend/modules/alarm-kit/ios, which kept the entire native module out of the repository.
/frontend/ios/
/frontend/android/
*.log
.DS_Store

# Expo generates its own frontend/.gitignore on every prebuild and owns what goes in
# it (expo-env.d.ts today). Do not copy those patterns here — they drift apart.