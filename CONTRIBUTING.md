## 앱 실행

```bash
cd frontend
npm install
npm run build:sim   # 시뮬레이터에 dev client 빌드 (처음 한 번, 그리고 네이티브 변경 후)
npm run dev         # Metro — 별도 터미널에 띄워둘 것
npm run sim         # 시뮬레이터에서 앱 실행
```


## 요구 사항

- **iOS 26 이상**
- **iOS SDK 26.1 이상을 포함한 Xcode** 
- **알람은 실기기에서만 실제로 울림.**
